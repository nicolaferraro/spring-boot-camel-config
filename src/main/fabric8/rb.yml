apiVersion: "v1"
kind: "RoleBinding"
metadata:
  name: "fis-camel-config"
roleRef:
  # Add the view role to the service account
  name: "view"
subjects:
  - kind: "ServiceAccount"
    name: "fis-camel-config"
